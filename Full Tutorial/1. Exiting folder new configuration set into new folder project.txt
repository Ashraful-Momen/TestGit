# Navigate to your new project folder
cd /path/to/your/new/project

# Run these commands step by step:

# 1. Initialize git if needed
git init

# 2. Configure Git (without SVN)
git config user.email "ashraful@instasure.xyz"
git config user.name "Ashraful Momen"
git config http.sslverify false
git config pull.rebase false

# 3. Add GitLab remote
git remote add origin http://119.148.19.197/ashraful/instasure-microsite-main.git

# 4. Create and switch to new branch
git checkout -b laravel_docker

# 5. Add all your files
git add .

# 6. Commit with descriptive message
git commit -m "Initial commit: Laravel Docker setup with complete infrastructure

- Added Docker configuration with PHP 8.3-fpm
- Implemented Docker-Compose with nginx, MySQL, Redis, phpMyAdmin
- Configured Laravel 10 with PHP 8.3
- Set up development environment with volume persistence
- Added security configurations with non-root user setup"

# 7. Push to GitLab
git push -u origin laravel_docker
